version: '3.6'
services:
  db:
    image: postgres:12.4
    restart: always
    environment:
      POSTGRES_PASSWORD: awesomepostgres

  adminer:
    image: adminer
    restart: always
    ports:
      - 8081:8080
